{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CHIP-8 RS debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/chip8rs",
            "args": ["--rom", "slipperyslope.ch8"],
            "cwd": "${workspaceRoot}",
        }
    ]
}